---
- name: Remove NTP
  yum:
    name=ntp
    state=absent
- name: Install Chrony
  yum:
    name=chrony
    state=present
- name: Setup chrony.conf
  copy:
    owner=root
    group=root
    mode=644
    src=chrony.conf
    dest=/etc/chrony.conf
  notify: Restart Chrony
