#!/bin/bash

echo "Starting master {{ master }}"
ssh -t {{ master }} "sudo systemctl start hbase-master";
{% for regionserver in regionservers %}
	echo "Starting regionserver {{ regionserver }}";
	ssh -t {{ regionserver }} "sudo systemctl start hbase-regionserver";
{% endfor %}
